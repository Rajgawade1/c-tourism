import { ReplaceExtPipePipe } from './replace-ext-pipe.pipe';

describe('ReplaceExtPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceExtPipePipe();
    expect(pipe).toBeTruthy();
  });
});
