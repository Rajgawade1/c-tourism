import { SortSilderPipePipe } from './sort-silder-pipe.pipe';

describe('SortSilderPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SortSilderPipePipe();
    expect(pipe).toBeTruthy();
  });
});
